# Background Orientation Notes

- 朝・夜の背景元画像はいずれも、窓・屋外景色が上、畳が下となる正しい縦構図で保存されている。
- ホーム画面で見えていた上下反転は、CSS背面ではなく前面のBabylon `roomLayer` に設定された `uScale = -1` と `vScale = -1` による180度反転が原因である。
- 今後の背景表示は、素材をそのままの向きで使い、Babylon背景テクスチャへ上下反転を適用しない。
