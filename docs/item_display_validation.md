# 防衛アイテム表示の確認記録

## 残り時間表示

蚊取り線香とだるまを個別の確認画面で設置し、両方の残り時間表示が右下に配置された18pxの小型リングとして描画されることを確認した。計算済みスタイルは `right: 2px`、`bottom: -4px` である。

## 蚊取り線香の回転

蚊取り線香の設置体本体ではなく、DOM上の `.incense-coil` だけに `incense-coil-turn`（5.6秒・線形・無限）を適用した。旧来のBabylon側のアイテム全体回転は削除済みである。

## 自動検証

Vitest 7件、型検査、本番ビルドが成功している。
