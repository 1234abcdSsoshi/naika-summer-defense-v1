# 捕食間隔・描画順・案内削除：確認記録

カエル確認画面で、得点・寝息・小判・防衛アイテムが表示される一方、「命中！」「指先で回収」の案内は表示されないことを確認した。

実行時の計算済みレイヤー値は、蚊が `z-index: 6`、小判が `z-index: 4`、設置アイテムが `z-index: 3` であった。したがって、蚊は小判と防衛アイテムの前面へ描画される。

カエルの捕食待機は `1.7` 秒から `0.85` 秒へ短縮し、回帰テストで定数値を確認した。
