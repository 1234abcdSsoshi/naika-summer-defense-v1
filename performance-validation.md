# 敵高密度時の軽量化検証記録

実施日: 2026-08-19

特殊蚊、危険物、小判が重なる局面でのメインスレッド負荷を抑えるため、ゲーム内部のシミュレーションは維持したまま、React／DOMへの表示同期を負荷に応じて **0.10秒、0.14秒、0.20秒** に動的調整した。敵、危険物、小判のDOMレイヤーは独立してメモ化し、無関係なHUD更新による再計算を避けている。敵の非表示Babylonスプライト生成・変形更新、毎フレームのモスキート音距離ソートも取り除いた。

高密度時は、蚊の最大同時数を10体、危険物を4個に制限し、群飛蚊の追加小蚊も上限を越えない方式に変更した。画面上では高密度クラスが不要な敵・危険物アニメーション、粒状合成、背景の`backdrop-filter`を停止し、位置移動はGPU合成向きの`translate3d`へ統一している。

確認結果は以下の通りである。

| 確認項目 | 結果 |
| --- | --- |
| 型検査 | `pnpm check` 成功 |
| 回帰テスト | Vitest 45件成功 |
| 本番ビルド | `pnpm build` 成功 |
| 375pxモバイル画面 | 群飛蚊デモ画面でHUD、風鈴、防衛アイテム、通知の崩れなし |

本番ビルドでは既存のBabylon.js由来の大きなチャンク警告だけが継続している。これはビルド失敗ではなく、今回の敵増加時に発生していたDOM更新負荷とは独立した最適化候補である。

## 表示回帰の追記

2026-08-19に`?demo&inspect&mosquito-type-check=swarm`で再確認したところ、寝息が減少するため敵のシミュレーションと攻撃は継続している一方、敵のDOMスプライトだけが画面に見えない状態を再現した。直前に位置指定を`vw`／`dvh`ベースの`translate3d`へ変更したことが、ゲーム領域基準の座標をビューポート基準へ誤変換している有力な原因である。位置指定はゲーム領域に対するパーセント方式へ戻し、GPU合成は内側の画像要素の変形だけに限定する。
